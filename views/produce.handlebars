    {{!-- {{#if loggedIn}} --}}
        <section class="bg-dark text-light d-lg-flex justify-content-center">
      <div class="container">
        <div class="row row-cols-1 row-cols-md-4 g-4 my-5">
          <div class="card-list">
            {{> product-card}}
          </div>
        </div>
      </div>
        </section> 
        {{!-- {{/if}} --}}